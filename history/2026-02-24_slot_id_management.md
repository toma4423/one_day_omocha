# 修正記録: 2026-02-24 (スロットID管理とUI改善)

## 29. 図柄管理のID（数字）ベースへの移行
絵文字の入力が困難な環境でも確実に設定が行えるよう、内部の識別子をテキストから数字（ID）に変更しました。

### コアロジックの刷新 (`src/utils/slot.py`)
- `DEFAULT_SYMBOLS` に `id` フィールドを追加。
- 役の判定 (`evaluate_slot_spin`) をID同士の比較に変更。
- 既存データからの移行ロジックを `get_slot_config` に実装（テキストパターンを自動的にIDパターンに置換）。

### 作成ページのUI改善 (`pages/9_⚙️_スロット作成.py`)
- **ID編集機能**: 各図柄に好きな数字IDを割り振ることが可能に。
- **プルダウンの強化**: 役の設定時、`1: 🍒` のようにIDと表示名がセットになったリストから選択できるようになり、視認性と入力性が向上。
- **自動計算の精度向上**: 全体の当り確率と個別の役の％指定に基づき、より正確に重みを算出。

### 品質管理
- 全34項目のテストがパス。
- Mypy による厳格な型チェックをクリア。
