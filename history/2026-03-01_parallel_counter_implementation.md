# 2026-03-01 並列カウンターの実装

## 概要
複数の項目を個別にカウントできる「並列カウンター」を「🛠 サポート・ツール系」として追加しました。

## 変更内容
- **ロジックの実装 (`src/utils/parallel_counter.py`)**:
    - 複数のカウンター（ID, 名前, 数値）を管理するロジックを実装。
    - 項目の追加、削除、数値の増減、名前の更新機能をカプセル化。
- **UI の実装 (`pages/25_🔢_並列カウンター.py`)**:
    - グリッド（3列）形式でカウンターを表示する UI を構築。
    - 個別のリセット機能、全体の削除・リセット機能を追加。
    - `SafeStorage` を使用し、ブラウザをリロードしても状態が維持されるようにしました。
- **テストの追加 (`tests/test_parallel_counter.py`)**:
    - ロジック層に対するユニットテストを 5 件追加し、正常動作を確認。
- **トップページ (`app.py`) の更新**:
    - サポート・ツール系の紹介リストに「並列カウンター」を追加。

## 確認事項
- [x] カウンターの追加・削除が正常に動作する。
- [x] 数値の加算・減算、名前の編集が LocalStorage に即座に反映される。
- [x] すべてのユニットテスト（65件）がパスしている。
- [x] リンターおよび型チェックに合格している。
