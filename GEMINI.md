# Gemini プロジェクトルール (GEMINI.md)

このファイルは、本プロジェクトにおいて AI エージェント（Gemini）が作業を行う際の絶対的なガイドラインを定めたものです。

## 1. アーキテクチャの原則
- **UIとロジックの完全分離**:
    - 計算、判定、データ処理などのコアロジックは、必ず `src/utils/` 内に純粋な Python 関数として実装する。
    - `src/utils/` 内（`styles.py` を除く）では、`streamlit` をインポートしてはならない。UIに依存しない独立したテストを可能にするため。
- **状態管理の統一**:
    - ブラウザの LocalStorage へのアクセスは、必ず `src/utils/storage.py` の `SafeStorage` クラスを介して行う。直接 `LocalStorage()` を各ページで操作しない。

## 2. 開発ワークフロー
- **テスト駆動の維持**:
    - 新機能の追加、または既存機能の修正時には、必ず `tests/` 内に対応するテストコードを追加または更新する。
    - 提出前に必ず `uv run pytest` を実行し、既存のテストを含め全件パスすることを確認する。
- **品質チェックの自動化**:
    - 作業完了前に、以下のコマンドを必ず実行し、エラーがないことを確認する。
        - `uv run ruff check . --fix` (リンターによる自動修正)
        - `uv run ruff format .` (コードフォーマット)
        - `uv run mypy src` (静的型チェック)

## 3. UI/UX デザインガイドライン
- **コンポーネントの再利用**:
    - グリッド状の盤面（マインスイーパー、双六、黒ひげ等）を作成する際は、`src/utils/styles.py` の `render_grid_board` を使用する。
- **共通要素の配置**:
    - 全てのおもちゃページ (`pages/*.py`) のサイドバー最下部には、必ず `render_donation_box(..., is_sidebar=True)` を配置し、開発支援リンクを表示する。

## 4. 記録と透明性
- **修正記録の作成**:
    - 重要な変更や機能追加を行った際は、`history/YYYY-MM-DD_feature_name.md` 形式で記録ファイルを作成し、変更理由と内容を明文化する。

---
これらのルールを遵守することで、プロジェクトの堅牢性、保守性、および一貫性を維持します。
