# Gemini プロジェクトルール (GEMINI.md)

このファイルは、本プロジェクトにおいて AI エージェント（Gemini）が作業を行う際の絶対的なガイドラインを定めたものです。**ここに記載されたルールは、いかなる場合も省略することは許されません。**

## 1. 開発の絶対フロー (最優先事項)
全てのコード変更において、以下の順序を厳守すること：
1.  **テストコードの作成・更新**: 機能を実装・修正する前に（あるいは同時に）、必ず `tests/` 内に対応するテストコードを用意する。
2.  **テストの実施とパス**: `uv run pytest` を実行し、変更した機能だけでなく**既存の全てのテスト**がパスすることを確認する。
3.  **品質チェック**: リンター（ruff）と型チェック（mypy）を実行し、エラーをゼロにする。
4.  **GitHub へのプッシュ**: 上記全てが完了した後に初めて GitHub へプッシュする。**テストが未実施、または失敗している状態でのプッシュは厳禁とする。**

## 2. アーキテクチャの原則
- **UIとロジックの完全分離**:
    - 計算、判定、データ処理などのコアロジックは、必ず `src/utils/` 内に純粋な Python 関数として実装する。
    - `src/utils/` 内（`styles.py` を除く）では、`streamlit` をインポートしてはならない。
- **状態管理の統一**:
    - ブラウザの LocalStorage へのアクセスは、必ず `src/utils/storage.py` の `SafeStorage` クラスを介して行う。
- **リロード対策の徹底**:
    - 画像アップロード等のリロードで消えるデータは、`SafeStorage` に一時退避し、ページ読み込み時に自動復元するロジックを組み込むこと。
- **ウィジェット更新の順序**:
    - `st.session_state` を介したウィジェット値の変更は、必ずそのウィジェットが描画される前に行い、必要に応じて `st.rerun()` を実行すること。

## 3. 開発ワークフロー
- **品質チェックの自動化**:
    - 提出前に必ず以下を実行すること：
        - `uv run ruff check . --fix`
        - `uv run ruff format .`
        - `uv run mypy src`

## 4. UI/UX デザインガイドライン
- **コンポーネントの再利用**:
    - グリッド状の盤面は `src/utils/styles.py` の `render_grid_board` を使用する。
- **外部アセットの管理**:
    - 大規模な HTML/JavaScript/CSS は、`src/assets/{toy_name}/` ディレクトリに分離し、メンテナンス性を確保する。
- **共通要素の配置**:
    - 全てのおもちゃページのサイドバー最下部には、必ず `render_donation_box(..., is_sidebar=True)` を配置する。

## 5. 記録と透明性
- **修正記録の作成**:
    - 重要な変更時は、`history/YYYY-MM-DD_feature_name.md` 形式で記録ファイルを作成し、変更理由と内容を明文化する。

## 6. 次世代移行に向けた設計指針 (API-First)
将来の FastAPI 移行および Kotlin 展開を確実にするため、以下の「疎結合ルール」を厳守すること：
- **Pydantic モデルの活用**:
    - 単純な値以外の入出力（複雑な辞書やリスト等）は、必ず Pydantic モデルまたは `dataclass` で定義する。
- **UI コンテキストの完全排除**:
    - `src/utils` 内の関数は、引数として渡されたデータのみを扱い、`st.session_state` や `st.query_params` を関数内で直接操作してはならない。
- **ステートレスな設計**:
    - ロジック関数は可能な限り副作用を持たず、同じ入力に対して常に同じ出力を返す（べき等性）ように設計する。
- **API互換のシグネチャ**:
    - 関数の戻り値は、そのまま JSON に変換可能な形式（基本型、モデル、またはそれらのリスト）とする。

---
これらのルールを遵守することで、プロジェクトの堅牢性、保守性、および一貫性を維持します。
